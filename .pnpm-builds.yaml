# pnpm approved builds (pnpm expected format)
# This file lists dependency build scripts allowed to run during `pnpm install`.
# Format: top-level key `allowed` with package names or objects {name, version}.

allowed:
  - name: "esbuild"
    version: "0.27.3"
  - name: "workerd"
    version: "1.20260903.1"
